前缀和的逆运算 
这个是将 a[] 数组作为和
d[i]=a[i]-a[i-1]

sum d = d[1]+d[2]+ ... +d[i]
    =a[1]-a[0]+a[2]-a[1]+ ... +a[i]-a[i-1]
    =a[i]-a[0]
a[0]=0 -> Sum d(i) = a[i]
//于是 a[i] 成了d[]数组的前缀和
    称d[]数组为差分数组

        d[2]+=1
所有 i >= 2 的 a[i] 都会加上一
    a[i] = sum d(i)+1
差分数组具有良好的修改性质 使原数组在某一区间内容易修改

